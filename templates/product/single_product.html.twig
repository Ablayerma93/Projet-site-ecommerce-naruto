<div class="product-item text-center">
    <a href="{{ path('app_product', { 'slug' : product.slug}) }}"><img src="assets/uploads/{{ product.illustration }}" alt="{{ product.name }}" height="200" style="border-radius: 15px;">
    <h5>{{ product.name}}</h5>
    <span class="product-subtitle">{{ product.subtitle}}</span>
    <span class="product-price"></span>{{ (product.price /100) |number_format(2, ',', '.') }} €</span>
</div>