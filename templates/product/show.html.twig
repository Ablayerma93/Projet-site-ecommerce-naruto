{% extends 'base.html.twig' %}

{% block title %}{{ product.name}} L'univers Naruto{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-5">
            <img src="/assets/uploads/{{ product.illustration}}" alt="{{product.name}}" class="img-fluid">
        </div>
    <div class="col-md-7 my-auto">
        <h2>{{product.name}}</h2>
        <p>{{product.subtitle}}</p>
       <span class="product-page-price">{{ (product.price /100) |number_format(2, ',', '.') }} €</span>
        <hr>
        <p>
            {{ product.description}}
        </p>
        <a href="{{ path('add_to_cart', {'id' : product.id}) }}" class="btn btn-warning">Ajoutez au panier</a>
    </div>
    

   
{% endblock %}
